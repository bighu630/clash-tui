# Rust 构建产物
/target

# 本地开发目录
.worktrees/
scratch/

# 临时文件
*.tmp
.validate.*

# pack.sh 打包产物（含订阅缓存/真实节点信息，严禁入库）
*.tar.gz

# 本地配置文件（含订阅 URL 与代理凭据，一律不入库）
settings.toml
subscriptions.toml
overrides.toml
/release.sh
/scripts/release.sh
