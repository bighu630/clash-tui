# 日志页 + 通知自动隐藏 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增第 5 个"日志"tab 实时展示 mihomo 日志流（级别过滤/滚动回溯/清空/1000 条环形缓冲），并改造底栏通知为"整组共享截止时间、到期同消、底栏动态收缩"。

**Architecture:** core/client.rs 新增 `LogLevel`/`LogEntry` 与 `log_stream()`（复用 LineStream 按行解析，兼容标准/structured 两种格式，解析失败降级原始文本）；新增 `spawn_logs_task` 后台任务（2s 重连 + 级别切换通道重连），日志进 `AppState.logs`（与 traffic 同模式）；`ui/logs.rs` 新增 LogsPage（仅持视图状态）；app.rs 通知改 `VecDeque<(Instant, String)>` + 组截止时间 + 动态底栏。

**Tech Stack:** Rust、ratatui 0.30、crossterm 0.28、tokio、reqwest、serde_json、futures-util。

**协调事项：** 本计划在独立 worktree `.worktrees/logs-page`（分支 `feature/logs-page`，基于 main）执行。进行中的 `refactor/groups-readonly` worktree（规则组重构，改 app.rs/client.rs/ui/groups.rs/merger.rs）与本计划改动的文件有重叠（app.rs/client.rs），但**互不在对方 worktree 内**，合并期手动 resolve。设计文档：`docs/superpowers/specs/2026-08-12-logs-page-design.md`。

---

## 文件结构

| 文件 | 变更 | 职责 |
|---|---|---|
| `src/core/client.rs` | 新增 `LogLevel`、`LogEntry`、`log_stream()` + 测试 | 日志模型与流式接口 |
| `src/app.rs` | 日志数据管道（logs 字段/事件/命令/后台任务）、tab 5、通知整组过期 + 动态底栏 + 测试 | 状态机与导航 |
| `src/ui/logs.rs` | 新建 `LogsPage` + 纯函数测试 | 日志页视图（级别/跟随/偏移） |
| `src/ui/mod.rs` | 注册 `pub mod logs;` | 模块声明 |
| `README.md` | 功能列表/使用指南/按键表 | 文档 |

**任务顺序**（每个任务结束都编译+测试全绿，无中间破坏状态）：

1. Task 1：client.rs 数据层
2. Task 2：app.rs 日志数据管道（不含 tab/页面挂载）
3. Task 3：ui/logs.rs 页面 + 模块注册
4. Task 4：app.rs tab 5 + 页面挂载 + 帮助/提示
5. Task 5：app.rs 通知整组过期 + 动态底栏
6. Task 6：README
7. Task 7：全量验证

---

## Task 1: core/client.rs — LogLevel/LogEntry/log_stream

**Files:**
- Modify: `src/core/client.rs`

- [ ] **Step 1: 新增类型与解析**

在 `MemoryFrame` 定义（约第 34-38 行）之后插入：

```rust
/// 日志级别（mihomo /logs?level= 阈值过滤，服务端下发 >= level 的日志）。
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum LogLevel {
    Error,
    Warning,
    Info,
    Debug,
}

impl LogLevel {
    pub fn as_str(self) -> &'static str {
        match self {
            LogLevel::Error => "error",
            LogLevel::Warning => "warning",
            LogLevel::Info => "info",
            LogLevel::Debug => "debug",
        }
    }

    /// 循环顺序 error → warning → info → debug（UI 按 e 键循环）。
    pub fn next(self) -> LogLevel {
        match self {
            LogLevel::Error => LogLevel::Warning,
            LogLevel::Warning => LogLevel::Info,
            LogLevel::Info => LogLevel::Debug,
            LogLevel::Debug => LogLevel::Error,
        }
    }

    /// 从 JSON 字符串解析；未知级别归为 info。
    pub fn from_str(s: &str) -> LogLevel {
        match s {
            "error" => LogLevel::Error,
            "warning" => LogLevel::Warning,
            "debug" => LogLevel::Debug,
            _ => LogLevel::Info,
        }
    }
}

/// 单条日志（来自 /logs）。time 仅 structured 格式提供（HH:MM:SS）。
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct LogEntry {
    pub time: Option<String>,
    pub level: LogLevel,
    pub message: String,
}

impl LogEntry {
    /// 解析一行日志，兼容两种格式：
    /// - 标准：{"type":"info","payload":"..."}
    /// - structured：{"time":"HH:MM:SS","level":"info","message":"...","fields":[]}
    /// 无法解析/缺关键字段时降级为 Debug 级原始文本（不丢日志）。
    pub fn parse(line: &str) -> LogEntry {
        let v: serde_json::Value = match serde_json::from_str(line) {
            Ok(v) => v,
            Err(_) => {
                return LogEntry {
                    time: None,
                    level: LogLevel::Debug,
                    message: line.to_string(),
                }
            }
        };
        if let Some(payload) = v.get("payload").and_then(|x| x.as_str()) {
            LogEntry {
                time: None,
                level: v
                    .get("type")
                    .and_then(|x| x.as_str())
                    .map(LogLevel::from_str)
                    .unwrap_or(LogLevel::Info),
                message: payload.to_string(),
            }
        } else if let Some(message) = v.get("message").and_then(|x| x.as_str()) {
            LogEntry {
                time: v.get("time").and_then(|x| x.as_str()).map(String::from),
                level: v
                    .get("level")
                    .and_then(|x| x.as_str())
                    .map(LogLevel::from_str)
                    .unwrap_or(LogLevel::Info),
                message: message.to_string(),
            }
        } else {
            LogEntry {
                time: None,
                level: LogLevel::Debug,
                message: line.to_string(),
            }
        }
    }
}
```

- [ ] **Step 2: 新增 log_stream**

在 `memory_stream`（约第 291-298 行）之后插入：

```rust
    /// GET /logs?level={level} → 按行解析的日志流；结束/出错由调用方重连。
    /// 与 /traffic、/memory 同模式：Bearer 鉴权 + LineStream 按行切分。
    pub async fn log_stream(
        &self,
        level: LogLevel,
    ) -> Result<impl Stream<Item = Result<LogEntry, ApiError>> + Unpin, ApiError> {
        let resp = self
            .stream_response(&format!("/logs?level={}", level.as_str()))
            .await?;
        let stream = LineStream::new(resp.bytes_stream().map(|c| c.map(|b| b.to_vec())));
        Ok(stream.map(|line| line.map(|l| LogEntry::parse(&l))))
    }
```

- [ ] **Step 3: 假服务器支持 /logs**

`spawn_api_server` 的 `let body = match path { ... }` 中，在 `"/traffic" => { ... }` 分支之后、`"/memory"` 分支之前新增（path 含查询串，用前缀匹配；与 `/traffic` 一样直接写流并 return）：

```rust
                        p if p.starts_with("/logs") => {
                            let payload = "{\"type\":\"info\",\"payload\":\"one\"}\n{\"time\":\"12:00:00\",\"level\":\"warning\",\"message\":\"two\",\"fields\":[]}\nnot-json-at-all\n";
                            let _ = sock
                                .write_all(
                                    format!(
                                        "HTTP/1.1 200 OK\r\nContent-Type: application/json\r\nContent-Length: {}\r\nConnection: close\r\n\r\n{payload}",
                                        payload.len()
                                    )
                                    .as_bytes(),
                                )
                                .await;
                            return;
                        }
```

- [ ] **Step 4: 新增单元测试**

在 `mod tests` 中、`client_base_url` 测试之后插入：

```rust
    // ---------- 日志解析 ----------

    #[test]
    fn log_entry_parse_standard_format() {
        let e = LogEntry::parse(r#"{"type":"info","payload":"[TCP] dial 1.2.3.4:443"}"#);
        assert_eq!(e.level, LogLevel::Info);
        assert_eq!(e.message, "[TCP] dial 1.2.3.4:443");
        assert_eq!(e.time, None);
    }

    #[test]
    fn log_entry_parse_structured_format() {
        let e = LogEntry::parse(
            r#"{"time":"12:34:56","level":"warning","message":"rule match","fields":[]}"#,
        );
        assert_eq!(e.level, LogLevel::Warning);
        assert_eq!(e.message, "rule match");
        assert_eq!(e.time.as_deref(), Some("12:34:56"));
    }

    #[test]
    fn log_entry_parse_unknown_level_falls_back_info() {
        let e = LogEntry::parse(r#"{"type":"verbose","payload":"x"}"#);
        assert_eq!(e.level, LogLevel::Info);
    }

    #[test]
    fn log_entry_parse_garbage_falls_back_debug_raw() {
        let e = LogEntry::parse("not json at all");
        assert_eq!(e.level, LogLevel::Debug);
        assert_eq!(e.message, "not json at all");
        assert_eq!(e.time, None);
    }

    #[test]
    fn log_entry_parse_json_without_known_keys_falls_back_raw() {
        let e = LogEntry::parse(r#"{"foo":1}"#);
        assert_eq!(e.level, LogLevel::Debug);
        assert_eq!(e.message, r#"{"foo":1}"#);
    }

    #[test]
    fn log_level_cycle_and_str() {
        assert_eq!(LogLevel::Error.next(), LogLevel::Warning);
        assert_eq!(LogLevel::Warning.next(), LogLevel::Info);
        assert_eq!(LogLevel::Info.next(), LogLevel::Debug);
        assert_eq!(LogLevel::Debug.next(), LogLevel::Error);
        assert_eq!(LogLevel::Info.as_str(), "info");
        assert_eq!(LogLevel::Debug.as_str(), "debug");
    }
```

- [ ] **Step 5: 新增流式联测**

在 `memory_stream_frame` 测试之后插入：

```rust
    #[tokio::test]
    async fn log_stream_parses_entries_and_sends_level_query() {
        let (port, mut rx) = spawn_api_server().await;
        let stream = client_on(port).log_stream(LogLevel::Warning).await.unwrap();
        let entries: Vec<LogEntry> = stream.take(3).map(|r| r.unwrap()).collect().await;
        assert_eq!(entries.len(), 3);
        assert_eq!(entries[0].level, LogLevel::Info);
        assert_eq!(entries[0].message, "one");
        assert_eq!(entries[1].level, LogLevel::Warning);
        assert_eq!(entries[1].time.as_deref(), Some("12:00:00"));
        assert_eq!(entries[2].level, LogLevel::Debug);
        assert_eq!(entries[2].message, "not-json-at-all");
        // 请求行应带 ?level= 查询参数与 Bearer 鉴权
        let req = rx.recv().await.expect("服务器应收到请求");
        assert!(
            req.starts_with("GET /logs?level=warning"),
            "请求行: {req}"
        );
        let req_lower = req.to_lowercase();
        assert!(
            req_lower.contains("authorization: bearer testsecret"),
            "应带 Bearer 鉴权: {req}"
        );
    }
```

- [ ] **Step 6: 运行测试**

Run: `cargo test core::client`
Expected: 全部通过（原 199 + 新增 7 = 206 通过）

- [ ] **Step 7: 提交**

```bash
git add src/core/client.rs
git commit -m "feat: client 新增 LogLevel/LogEntry 与 /logs 流式接口 log_stream"
```

---

## Task 2: app.rs — 日志数据管道（不含 tab/页面挂载）

**Files:**
- Modify: `src/app.rs`

本任务把日志数据流接入主循环，但**不**改 TABS/pages（Task 4 做），保持每个任务可编译。

- [ ] **Step 1: 导入与常量**

`use crate::core::client::{Client, ConnInfo, ConnSnapshot, MemoryFrame, RuntimeConfig, TrafficFrame};`（约第 20 行）改为：

```rust
use crate::core::client::{
    Client, ConnInfo, ConnSnapshot, LogEntry, LogLevel, MemoryFrame, RuntimeConfig, TrafficFrame,
};
```

`const CONNECTIONS_POLL: Duration = Duration::from_secs(3);`（约第 154 行）之后加：

```rust
/// 日志保留上限（环形缓冲，超出淘汰最旧）。
const LOG_HISTORY: usize = 1000;
```

- [ ] **Step 2: AppState 增 logs 字段**

`pub notices: VecDeque<String>,`（约第 49 行）之后加：

```rust
    /// mihomo 日志环形缓冲（logs 后台任务填充，日志页只读展示）。
    pub logs: VecDeque<LogEntry>,
```

`AppState::load()` 的 `Self { ... }` 中 `notices,` 前加 `logs: VecDeque::new(),`。

- [ ] **Step 3: UiEvent 与 UiCommand 扩展**

`pub enum UiEvent` 中 `ConfigsRefreshed(Result<RuntimeConfig, String>),`（约第 159 行附近）之后加：

```rust
    /// logs 后台任务推送的单条日志。
    LogLine(LogEntry),
```

`pub enum UiCommand` 中 `InstallSetup,` 之后加：

```rust
    /// 日志页切换显示级别：主循环转发给 logs 后台任务触发 ?level= 重连。
    SetLogLevel(LogLevel),
```

`spawn_command` 的 match 中 `UiCommand::InstallSetup => { ... }` 分支之后加：

```rust
            UiCommand::SetLogLevel(level) => {
                let _ = self.log_level_tx.send(level);
            }
```

`on_ui_event` 的 match 中 `UiEvent::ConfigsRefreshed(res) => match res { ... },` 分支之后加：

```rust
            UiEvent::LogLine(entry) => self.on_log(entry),
```

- [ ] **Step 4: on_log + spawn_logs_task**

`on_memory` 方法之后加：

```rust
    fn on_log(&mut self, entry: LogEntry) {
        if self.state.logs.len() >= LOG_HISTORY {
            self.state.logs.pop_front();
        }
        self.state.logs.push_back(entry);
    }
```

`spawn_memory_task` 之后加（与 traffic 任务同模式；级别变化时立即重连）：

```rust
/// logs 后台任务：流式拉取 /logs?level=，失败/断开 sleep 2s 重连；
/// 级别切换经 level_rx 通知，立即以新级别重连。
fn spawn_logs_task(
    client: Arc<Client>,
    mut level_rx: mpsc::UnboundedReceiver<LogLevel>,
    tx: mpsc::UnboundedSender<LogEntry>,
) {
    tokio::spawn(async move {
        let mut level = LogLevel::Info;
        loop {
            let mut stream = match client.log_stream(level).await {
                Ok(s) => s,
                Err(_) => {
                    tokio::time::sleep(Duration::from_secs(2)).await;
                    continue;
                }
            };
            loop {
                tokio::select! {
                    new_level = level_rx.recv() => match new_level {
                        Some(l) => {
                            level = l;
                            break; // 以新级别重连
                        }
                        None => return, // 主循环已退出
                    },
                    item = stream.next() => match item {
                        Some(Ok(entry)) => {
                            if tx.send(entry).is_err() {
                                return;
                            }
                        }
                        Some(Err(_)) | None => break, // 断开 → 外层重连
                    },
                }
            }
            tokio::time::sleep(Duration::from_secs(2)).await;
        }
    });
}
```

> `log_stream` 返回 `impl Stream + Unpin`，`stream.next()` 在 select 中直接可用（futures_util::StreamExt，已导入）。

- [ ] **Step 5: App 结构体与 run_loop**

`App` 结构体中 `exit_trigger: mpsc::UnboundedSender<()>,` 之后加：

```rust
    /// 日志级别切换通道（spawn_command → spawn_logs_task）。
    log_level_tx: mpsc::UnboundedSender<LogLevel>,
```

`run_loop` 签名（约第 386 行）改为：

```rust
    async fn run_loop(
        &mut self,
        mut traffic_rx: mpsc::UnboundedReceiver<BgMsg>,
        mut memory_rx: mpsc::UnboundedReceiver<MemoryFrame>,
        mut conns_rx: mpsc::UnboundedReceiver<ConnSnapshot>,
        mut logs_rx: mpsc::UnboundedReceiver<LogEntry>,
        mut ui_rx: mpsc::UnboundedReceiver<UiEvent>,
        mut sudo_rx: mpsc::UnboundedReceiver<String>,
    ) -> Result<(), BoxError> {
```

`enum Act` 中加 `Log(LogEntry),`；select! 中 `msg = conns_rx.recv() => ...` 之后加：

```rust
                msg = logs_rx.recv() => match msg { Some(m) => Act::Log(m), None => continue },
```

match act 中 `Act::Conns(snap) => self.on_conns(snap),` 之后加：

```rust
                Act::Log(entry) => self.on_log(entry),
```

- [ ] **Step 6: run() 接线**

`run()` 中 `let (conns_tx, conns_rx) = mpsc::unbounded_channel();` 之后加：

```rust
    let (log_tx, log_rx) = mpsc::unbounded_channel();
    let (log_level_tx, log_level_rx) = mpsc::unbounded_channel();
```

`spawn_exit_ip_task(exit_ports.clone(), trigger_rx, ui_tx.clone());` 之后加：

```rust
    spawn_logs_task(client.clone(), log_level_rx, log_tx);
```

`App { ... }` 构造中 `exit_trigger,` 之后加 `log_level_tx,`。

`run_loop` 调用改为：

```rust
    let result = app
        .run_loop(traffic_rx, memory_rx, conns_rx, log_rx, ui_rx, sudo_rx)
        .await;
```

- [ ] **Step 7: test_app_with_width 适配**

`let (exit_trigger, _) = mpsc::unbounded_channel();` 之后加：

```rust
        let (log_level_tx, _) = mpsc::unbounded_channel();
```

`AppState { ... }` 中 `notices: VecDeque::new(),` 前加 `logs: VecDeque::new(),`。

`App { ... }` 构造中 `exit_trigger,` 之后加 `log_level_tx,`。

- [ ] **Step 8: 新增数据流测试**

`mod tests` 末尾追加：

```rust
    /// 日志环形缓冲：超过 LOG_HISTORY 淘汰最旧。
    #[test]
    fn logs_push_truncates_at_cap() {
        let mut app = test_app(24);
        for i in 0..(LOG_HISTORY + 100) {
            app.on_log(LogEntry {
                time: None,
                level: LogLevel::Info,
                message: format!("m{i}"),
            });
        }
        assert_eq!(app.state.logs.len(), LOG_HISTORY);
        assert_eq!(app.state.logs.front().unwrap().message, "m100");
        assert_eq!(
            app.state.logs.back().unwrap().message,
            format!("m{}", LOG_HISTORY + 99)
        );
    }

    /// SetLogLevel 命令转发到 logs 后台任务通道。
    #[test]
    fn set_log_level_forwards_to_task_channel() {
        let (tx, mut rx) = mpsc::unbounded_channel();
        let mut app = test_app(24);
        app.log_level_tx = tx;
        app.spawn_command(UiCommand::SetLogLevel(LogLevel::Debug));
        assert_eq!(rx.try_recv(), Ok(LogLevel::Debug));
    }
```

- [ ] **Step 9: 运行测试**

Run: `cargo test`
Expected: 全部通过

- [ ] **Step 10: 提交**

```bash
git add src/app.rs
git commit -m "feat: app 接入 /logs 数据流（后台任务+级别重连+1000条环形缓冲）"
```

---

## Task 3: ui/logs.rs — LogsPage（含模块注册）

**Files:**
- Create: `src/ui/logs.rs`
- Modify: `src/ui/mod.rs`

- [ ] **Step 1: 创建 src/ui/logs.rs（完整页面）**

```rust
//! 日志页：实时日志流展示（只读）。数据来自 AppState.logs（主循环后台任务
//! 填充），本页仅持有视图状态：当前级别、跟随/回溯偏移。

use crossterm::event::{KeyCode, KeyEvent};
use ratatui::layout::Rect;
use ratatui::style::{Color, Modifier, Style};
use ratatui::text::{Line, Span};
use ratatui::widgets::{Block, Borders, Paragraph};
use ratatui::Frame;

use crate::app::{AppState, UiCommand};
use crate::core::client::{LogEntry, LogLevel};
use crate::ui::Page;

/// 日志页视图状态。
pub struct LogsPage {
    /// 当前显示级别（经 UiCommand::SetLogLevel 通知后台任务重连 /logs?level=）。
    pub level: LogLevel,
    /// 是否跟随底部；回溯（↑↓/PgUp/PgDn）时暂停，f/End 恢复。
    pub follow: bool,
    /// 回溯偏移：距最新一条日志的条数（0 = 最新）。
    pub offset: usize,
}

impl LogsPage {
    pub fn new() -> Self {
        Self {
            level: LogLevel::Info,
            follow: true,
            offset: 0,
        }
    }

    /// 向上回溯 n 行（暂停跟随）。
    fn scroll_up(&mut self, n: usize) {
        self.follow = false;
        self.offset = self.offset.saturating_add(n);
    }

    /// 向下回溯 n 行；回到最新（offset=0）时恢复跟随。
    fn scroll_down(&mut self, n: usize) {
        self.follow = false;
        self.offset = self.offset.saturating_sub(n);
        if self.offset == 0 {
            self.follow = true;
        }
    }

    /// 复位视图到底部跟随。
    fn reset_view(&mut self) {
        self.follow = true;
        self.offset = 0;
    }

    /// 可见日志区间 [start, end)（纯函数，供测试）：
    /// follow=true → 最后 min(total, rows) 条；否则从 offset 处向上取 rows 条。
    pub fn visible_range(total: usize, rows: usize, follow: bool, offset: usize) -> (usize, usize) {
        if total == 0 || rows == 0 {
            return (0, 0);
        }
        let end = total.saturating_sub(if follow { 0 } else { offset.min(total) });
        let start = end.saturating_sub(rows);
        (start, end)
    }

    /// 级别样式（纯函数，供测试）。
    pub fn level_style(level: LogLevel) -> Style {
        match level {
            LogLevel::Error => Style::default().fg(Color::Red),
            LogLevel::Warning => Style::default().fg(Color::Yellow),
            LogLevel::Info => Style::default(),
            LogLevel::Debug => Style::default().fg(Color::Gray),
        }
    }

    /// 单条日志显示行（纯函数，供测试）：structured 带时间前缀，标准格式无时间。
    pub fn format_entry(e: &LogEntry) -> Line<'static> {
        let mut spans = Vec::new();
        if let Some(t) = &e.time {
            spans.push(Span::styled(
                format!("{t} "),
                Style::default().fg(Color::DarkGray),
            ));
        }
        spans.push(Span::styled(e.message.clone(), Self::level_style(e.level)));
        Line::from(spans)
    }
}

impl Page for LogsPage {
    fn handle_key(&mut self, key: KeyEvent, st: &mut AppState) -> Option<UiCommand> {
        match key.code {
            KeyCode::Char('e') => {
                self.level = self.level.next();
                self.reset_view();
                Some(UiCommand::SetLogLevel(self.level))
            }
            KeyCode::Char('c') => {
                st.logs.clear();
                self.reset_view();
                None
            }
            KeyCode::Char('f') | KeyCode::End => {
                self.reset_view();
                None
            }
            KeyCode::Up => {
                self.scroll_up(1);
                None
            }
            KeyCode::Down => {
                self.scroll_down(1);
                None
            }
            KeyCode::PageUp => {
                self.scroll_up(10);
                None
            }
            KeyCode::PageDown => {
                self.scroll_down(10);
                None
            }
            _ => None,
        }
    }

    fn render(&mut self, f: &mut Frame, area: Rect, st: &AppState) {
        let title = format!(
            " 日志  [{}]  {} 条 {} ",
            self.level.as_str(),
            st.logs.len(),
            if self.follow { "跟随中" } else { "已暂停" }
        );
        let block = Block::new()
            .title(Span::styled(
                title,
                Style::default().fg(Color::Cyan).add_modifier(Modifier::BOLD),
            ))
            .borders(Borders::ALL);
        let inner = block.inner(area);
        let rows = inner.height as usize;
        let (start, end) = Self::visible_range(st.logs.len(), rows, self.follow, self.offset);
        if start == end {
            let msg = Paragraph::new("等待 mihomo 日志……（按 e 切换级别）")
                .style(Style::default().fg(Color::DarkGray))
                .block(block);
            f.render_widget(msg, area);
            return;
        }
        let lines: Vec<Line> = st
            .logs
            .iter()
            .skip(start)
            .take(end - start)
            .map(Self::format_entry)
            .collect();
        f.render_widget(Paragraph::new(lines).block(block), area);
    }
}
```

- [ ] **Step 2: ui/mod.rs 注册模块**

`src/ui/mod.rs` 中 `pub mod dashboard;` 前加：

```rust
pub mod logs;
```

- [ ] **Step 3: 新增页面单元测试（追加到 src/ui/logs.rs 末尾）**

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn visible_range_follow_shows_tail() {
        assert_eq!(LogsPage::visible_range(100, 20, true, 0), (80, 100));
        assert_eq!(LogsPage::visible_range(10, 20, true, 0), (0, 10));
        assert_eq!(LogsPage::visible_range(0, 20, true, 0), (0, 0));
    }

    #[test]
    fn visible_range_scrolled_back() {
        // 回溯 30 条：显示 [50, 70)
        assert_eq!(LogsPage::visible_range(100, 20, false, 30), (50, 70));
        // 回溯超过总量：钳制到 [0, 20)
        assert_eq!(LogsPage::visible_range(100, 20, false, 500), (0, 20));
        // 偏移 0 但未跟随：等价于显示底部
        assert_eq!(LogsPage::visible_range(100, 20, false, 0), (80, 100));
    }

    #[test]
    fn level_style_maps() {
        assert_eq!(LogsPage::level_style(LogLevel::Error).fg, Some(Color::Red));
        assert_eq!(LogsPage::level_style(LogLevel::Warning).fg, Some(Color::Yellow));
        assert_eq!(LogsPage::level_style(LogLevel::Info).fg, None);
        assert_eq!(LogsPage::level_style(LogLevel::Debug).fg, Some(Color::Gray));
    }

    #[test]
    fn format_entry_structured_has_time_prefix() {
        let e = LogEntry {
            time: Some("12:00:00".into()),
            level: LogLevel::Warning,
            message: "boom".into(),
        };
        let line = LogsPage::format_entry(&e);
        let text: String = line.spans.iter().map(|s| s.content.as_ref()).collect();
        assert_eq!(text, "12:00:00 boom");
    }

    #[test]
    fn format_entry_standard_no_time() {
        let e = LogEntry {
            time: None,
            level: LogLevel::Info,
            message: "hi".into(),
        };
        let line = LogsPage::format_entry(&e);
        let text: String = line.spans.iter().map(|s| s.content.as_ref()).collect();
        assert_eq!(text, "hi");
    }

    #[test]
    fn scroll_up_pauses_follow() {
        let mut page = LogsPage::new();
        page.scroll_up(1);
        assert!(!page.follow);
        assert_eq!(page.offset, 1);
    }

    #[test]
    fn scroll_down_back_to_bottom_resumes_follow() {
        let mut page = LogsPage::new();
        page.scroll_up(5);
        page.scroll_down(3);
        assert!(!page.follow);
        assert_eq!(page.offset, 2);
        page.scroll_down(10);
        assert!(page.follow, "回到最新应恢复跟随");
        assert_eq!(page.offset, 0);
    }
}
```

- [ ] **Step 4: 运行测试**

Run: `cargo test`
Expected: 全部通过

- [ ] **Step 5: 提交**

```bash
git add src/ui/logs.rs src/ui/mod.rs
git commit -m "feat: 日志页 LogsPage（级别切换/滚动回溯/清空/跟随）"
```

---

## Task 4: app.rs — tab 5 + 页面挂载 + 帮助/提示

**Files:**
- Modify: `src/app.rs`

- [ ] **Step 1: TABS 与数字键**

`const TABS: [&str; 4] = ["仪表盘", "订阅", "规则组", "规则"];` 改为：

```rust
const TABS: [&str; 5] = ["仪表盘", "订阅", "规则组", "规则", "日志"];
```

`handle_key` 中 `KeyCode::Char(c) if ('1'..='4').contains(&c)` 改为 `('1'..='5')`。

- [ ] **Step 2: HELP_LINES**

`HELP_LINES` 中 `"规则:"` 小节（`"  d                  删除规则",`）之后加：

```rust
    "",
    "日志:",
    "  e                  切换级别 (error → warning → info → debug)",
    "  ↑ / ↓ / PgUp / PgDn 回溯日志（暂停跟随）",
    "  f / End             恢复跟随底部",
    "  c                  清空日志",
```

- [ ] **Step 3: page_hints**

`page_hints` 中 `_ => vec![`（规则页兜底分支）改为 `3 => vec![`（内容不变），并在其后新增：

```rust
        4 => vec![
            ("e".into(), "级别".into()),
            ("c".into(), "清空".into()),
            ("f".into(), "跟随".into()),
            ("↑↓".into(), "滚动".into()),
        ],
```

- [ ] **Step 4: pages 挂载（run() 与 test_app_with_width）**

`run()` 的 `pages` 数组末尾（`Box::new(RulesPage::new()),` 之后）加：

```rust
        Box::new(crate::ui::logs::LogsPage::new()),
```

`test_app_with_width` 的 `pages` 数组同样末尾加一行相同代码。

- [ ] **Step 5: 新增导航测试**

`mod tests` 末尾追加：

```rust
    /// 数字键 5 切到日志页（index 4）。
    #[test]
    fn tab_key_5_switches_to_logs_page() {
        let mut app = test_app(24);
        assert_eq!(app.pages.len(), 5);
        let _ = app.handle_key(KeyEvent::new(KeyCode::Char('5'), KeyModifiers::NONE));
        assert_eq!(app.current, 4);
    }

    /// 日志页 handle_key('e') 发 SetLogLevel 命令并循环级别。
    #[test]
    fn logs_page_e_key_cycles_level() {
        let mut app = test_app(24);
        app.current = 4;
        let cmd = app.pages[4].handle_key(
            KeyEvent::new(KeyCode::Char('e'), KeyModifiers::NONE),
            &mut app.state,
        );
        assert!(matches!(cmd, Some(UiCommand::SetLogLevel(LogLevel::Warning))));
    }
```

- [ ] **Step 6: 运行测试**

Run: `cargo test`
Expected: 全部通过

- [ ] **Step 7: 提交**

```bash
git add src/app.rs
git commit -m "feat: tab 导航 4→5 挂载日志页（帮助/按键提示同步）"
```

---

## Task 5: app.rs — 通知整组过期 + 动态底栏

**Files:**
- Modify: `src/app.rs`

- [ ] **Step 1: 通知 TTL 常量与纯函数**

`const API_NOTICE_DEBOUNCE: Duration = Duration::from_secs(3);` 之后加：

```rust
/// 通知类型时长：`[✗]`/`[!]` 错误警告 10s，其余（成功/普通）5s。
const NOTICE_OK_TTL: Duration = Duration::from_secs(5);
const NOTICE_ERR_TTL: Duration = Duration::from_secs(10);

/// 单条通知的类型时长。
fn notice_ttl(text: &str) -> Duration {
    if text.starts_with("[✗]") || text.starts_with("[!]") {
        NOTICE_ERR_TTL
    } else {
        NOTICE_OK_TTL
    }
}

/// 整组通知截止时间：组内所有 `到达时刻 + 类型时长` 的最大值
/// （锚定"时间最长的一条"，不管新旧）；空组返回 None。
/// 调用方以 `deadline > now` 判定整组是否可见，到期整组同时消失。
fn notice_deadline(notices: &[(Instant, String)]) -> Option<Instant> {
    notices
        .iter()
        .map(|(at, text)| at.saturating_add(notice_ttl(text)))
        .max()
}
```

- [ ] **Step 2: AppState.notices 类型与 notice()**

`pub notices: VecDeque<String>,` 改为：

```rust
    /// 通知（带到达时刻）。整组共享截止时间（notice_deadline），到期整组同消。
    pub notices: VecDeque<(Instant, String)>,
```

`notice()` 改为：

```rust
    /// 追加通知（记录到达时刻），保留最近 5 条。
    pub fn notice(&mut self, msg: String) {
        self.notices.push_back((Instant::now(), msg));
        while self.notices.len() > 5 {
            self.notices.pop_front();
        }
    }
```

`AppState::load()` 中三处 `notices.push_back(format!(...))`（约第 58/65/72 行）改为 `notices.push_back((Instant::now(), format!(...)))`。

- [ ] **Step 3: draw() 动态底栏**

`draw()` 中，删除预计算 `let notices: Vec<(String, bool)> = ...` 整段（约第 228-235 行），改为：

```rust
        let now = Instant::now();
        // 整组共享截止时间：到期整组同时清除（避免一条条过期触发多次重绘）
        let notices_visible = notice_deadline(&self.state.notices).is_some_and(|d| d > now);
        if !notices_visible {
            self.state.notices.clear();
        }
        let visible_notice_rows = if notices_visible {
            self.state.notices.len().min(3)
        } else {
            0
        };
```

底栏布局 `Constraint::Length(4)` 改为：

```rust
            let [top, middle, bottom] = Layout::vertical([
                Constraint::Length(3),
                Constraint::Min(1),
                Constraint::Length(1 + visible_notice_rows as u16),
            ])
            .areas(area);
```

通知渲染循环（原 `for (i, (text, ok)) in notices.iter().take(...)`）改为：

```rust
            let (notice_rows, hint_y) = bottom_bar_rows(bottom, area.height);
            for (i, (_, text)) in st.notices.iter().rev().take(notice_rows as usize).enumerate() {
                let style = if text.starts_with("[✓]") {
                    Style::default().fg(Color::Green)
                } else if text.starts_with("[!]") {
                    Style::default().fg(Color::Yellow)
                } else {
                    Style::default().fg(Color::Red)
                };
                f.render_widget(
                    Paragraph::new(Span::styled(text.clone(), style)),
                    Rect::new(bottom.x, bottom.y + i as u16, bottom.width, 1),
                );
            }
```

> 闭包内仍用 `st`（`let st = &self.state;` 在闭包内已定义，仅不可变读取）。`self.state.notices` 的清除发生在闭包外。

- [ ] **Step 4: 更新既有测试的类型适配**

`mod tests` 中四处断言 `.iter().any(|n| n.contains(...))` 改为 `.iter().any(|(_, t)| t.contains(...))`：
- `patch_done_ok_saved_notices_success`
- `patch_done_ok_not_saved_notices_warning`
- `exit_ip_recovery_closes_stale_popup_and_notices`（两处）
- `exit_ip_recovery_keeps_unrelated_popup`（一处）

`bottom_bar_rows_always_in_bounds` 从固定 `Length(4)` 改为遍历可见通知数 0..=3：

```rust
    #[test]
    fn bottom_bar_rows_always_in_bounds() {
        for n in 0..=3u16 {
            for h in 0..=30u16 {
                let area = Rect::new(0, 0, 30, h);
                let [_, _, bottom] = Layout::vertical([
                    Constraint::Length(3),
                    Constraint::Min(1),
                    Constraint::Length(1 + n),
                ])
                .areas(area);
                let (notice_rows, hint_y) = bottom_bar_rows(bottom, h);
                assert!(notice_rows <= h, "n={n} h={h}: notice_rows {notice_rows} > 高度");
                match hint_y {
                    Some(y) => {
                        assert!(y < h, "n={n} h={h}: hint_y {y} >= 高度");
                        assert!(y >= bottom.y, "n={n} h={h}: hint_y {y} < bottom.y {}", bottom.y);
                        assert_eq!(
                            y - bottom.y,
                            notice_rows,
                            "n={n} h={h}: 通知行数应等于 hint_y - bottom.y"
                        );
                    }
                    None => assert_eq!(notice_rows, 0, "n={n} h={h}: 无提示行时通知也应为 0"),
                }
            }
        }
    }
```

> `draw_tiny_terminal_no_panic` / `draw_wide_terminal_connections_visible_no_panic` 无需改动（`notice()` 调用与断言不变，布局形状相同）。

- [ ] **Step 5: 新增通知过期测试**

`mod tests` 末尾追加：

```rust
    /// 组截止时间 = 组内「到达+时长」最大的一条（锚定时间最长，不管新旧）。
    #[test]
    fn notice_deadline_anchors_longest() {
        let now = Instant::now();
        // 仅成功通知：5s
        let d = notice_deadline(&[(now, "[✓] a".to_string())]).unwrap();
        assert_eq!(d, now + NOTICE_OK_TTL);
        // 错误通知：10s
        let d = notice_deadline(&[(now, "[✗] a".to_string())]).unwrap();
        assert_eq!(d, now + NOTICE_ERR_TTL);
        // 旧成功 + 新错误：锚定新错误的 10s
        let old = now - Duration::from_secs(3);
        let d = notice_deadline(&[
            (old, "[✓] old".to_string()),
            (now, "[✗] new".to_string()),
        ])
        .unwrap();
        assert_eq!(d, now + NOTICE_ERR_TTL);
        // 旧错误 + 新成功：仍锚定错误（到达早，但时长最长）
        let old_err = now - Duration::from_secs(2);
        let d = notice_deadline(&[
            (old_err, "[✗] old".to_string()),
            (now, "[✓] new".to_string()),
        ])
        .unwrap();
        assert_eq!(d, old_err + NOTICE_ERR_TTL);
        // 空组：None
        assert_eq!(notice_deadline(&[]), None);
    }

    /// 过期整组在 draw 时同时清除（不留半组）。
    #[test]
    fn expired_notices_cleared_on_draw() {
        let mut app = test_app(24);
        app.state.notices.push_back((
            Instant::now() - Duration::from_secs(60),
            "[✓] 旧通知".to_string(),
        ));
        app.state.notices.push_back((
            Instant::now() - Duration::from_secs(60),
            "[✗] 更旧".to_string(),
        ));
        let _ = app.draw().expect("draw 不应失败");
        assert!(app.state.notices.is_empty(), "过期整组应被清除");
    }

    /// 无通知时底栏收成 1 行（仅按键提示）；有通知时通知文本可见。
    #[test]
    fn bottom_bar_collapses_without_notices() {
        let mut app = test_app(24);
        let frame = app.draw().expect("draw 不应失败");
        let cell = frame.buffer.cell((0, 23)).expect("提示行应在最后一行");
        assert!(
            cell.symbol().starts_with('['),
            "无通知时最后一行应为按键提示: {:?}",
            cell.symbol()
        );
        // 有通知：通知文本渲染可见
        app.state.notice("[✓] 测试通知".to_string());
        let text = buffer_text(&mut app);
        assert!(text.contains("测试通知"), "通知应可见: {text}");
    }
```

- [ ] **Step 6: 运行测试**

Run: `cargo test`
Expected: 全部通过

- [ ] **Step 7: 提交**

```bash
git add src/app.rs
git commit -m "feat: 通知整组截止时间自动隐藏（✗/! 10s、其余 5s）+ 底栏动态收缩"
```

---

## Task 6: README 文档

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 功能列表**

找到功能列表小节，补一条：

```markdown
- **日志页**：实时日志流展示，级别过滤（error/warning/info/debug）、滚动回溯、一键清空
```

- [ ] **Step 2: 使用指南**

找到使用指南页面说明部分，新增"日志页"小节（参考现有页面小节风格）：

```markdown
### 日志页

实时展示 mihomo 运行日志（`GET /logs` 流式接口，断线自动重连）：

- **级别过滤**：按 `e` 在 error → warning → info → debug 间循环切换（服务端按阈值下发）
- **滚动**：默认自动跟随底部；`↑/↓` 逐行、`PgUp/PgDn` 翻页回溯时暂停跟随；`f` 或 `End` 恢复跟随
- **清空**：`c` 清空当前缓冲；缓冲上限 1000 条，超出自动淘汰最旧
- 日志在切换页面/退出后不保留（重启清空）
```

- [ ] **Step 3: 按键表与 Tab 说明**

- 按键速查表补日志页行：`| 日志页 | e / c / f / End / ↑↓ / PgUp / PgDn | 切换级别 / 清空 / 恢复跟随 / 滚动回溯 |`
- Tab 切换说明中 `1-4` 改为 `1-5`
- 若 README 有"底部通知栏"描述，补充"通知 5-10 秒自动隐藏、无通知时底栏收缩"一句

- [ ] **Step 4: 一致性检查**

Run: `grep -n "1-4\|1-5\|日志" README.md` 确认无残留旧描述。

- [ ] **Step 5: 提交**

```bash
git add README.md
git commit -m "docs: README 日志页功能/使用指南/按键表"
```

---

## Task 7: 全量验证

- [ ] **Step 1: 完整测试**

Run: `cargo test`
Expected: 全部通过（约 210+ 个）

- [ ] **Step 2: 构建与静态检查**

Run: `cargo build --release && cargo clippy --all-targets 2>&1 | tail -20`
Expected: 无新警告（若项目此前 clippy 不干净，报告差异即可，不强制清零）

- [ ] **Step 3: 端到端验证（尽力而为）**

1. 检查本机 mihomo：`curl -s http://127.0.0.1:9090/version`（或查看 settings.toml 的 external_controller/secret）
2. 若 mihomo 可达：`cargo run` 手动验证——Tab 切到日志页、观察日志滚动、按 e 切级别、↑ 回溯暂停、f 恢复、c 清空、触发一次"应用配置"观察通知 5s 消失、底栏收缩
3. 若 mihomo 不可达：记录结论，依赖假服务器联测（log_stream_parses_entries_and_sends_level_query）覆盖

- [ ] **Step 4: 汇报**

向 feature_lead 汇报：测试数、clippy 结果、端到端验证结论、提交列表（`git log --oneline -7`）。
